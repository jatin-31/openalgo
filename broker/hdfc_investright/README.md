# HDFC Investright Integration

This repository contains the integration files for HDFC Investright.

## Setup Instructions

1. Clone the repository.
2. Install required packages using `pip install -r requirements.txt`.
3. Update `config.json` with your API keys.
4. Run `hdfc_invest.py` for integration.
